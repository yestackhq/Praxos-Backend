# Praxos LMS backend (Python 3.9+)
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2
PyJWT[crypto]>=2.8
httpx>=0.27
python-multipart>=0.0.9

# Document pipeline: PDF text extraction, embeddings/scoring, pgvector storage
pypdf>=4.0
openai>=1.40
pgvector>=0.2.5

# Postgres / Supabase driver (uncomment for production)
# psycopg[binary]>=3.1

# Dev / test
pytest>=8
